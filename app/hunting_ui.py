from __future__ import annotations

from fastapi.responses import HTMLResponse

from app.build_info import APP_VERSION, BUILD_ID


def hunting_page() -> HTMLResponse:
    html = r'''<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>NCT · Hunt</title>
<style>
:root{color-scheme:dark;--bg:#08131b;--panel:#101d26;--line:#294553;--text:#edf6fb;--muted:#9eb0b8;--accent:#57d6bf;--blue:#73bfff;--good:#61d095;--warn:#f4c95d;--bad:#ff7b7b;--hunt-header-offset:132px}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at top right,#102b35,#08131b 48%);color:var(--text);font:14px/1.45 system-ui,-apple-system,Segoe UI,sans-serif}header,main{max-width:1280px;margin:auto}header{position:sticky;top:0;z-index:100;padding:16px 0 14px;border-bottom:1px solid var(--line);background:rgba(8,19,27,.97);backdrop-filter:blur(14px)}h1{margin:0 0 4px;font-size:26px}h2{margin:0 0 8px;font-size:19px}h3{margin:17px 0 8px;font-size:15px}.eyebrow{color:var(--accent);font-size:11px;font-weight:800;letter-spacing:.08em;text-transform:uppercase}.sub,.hint,.meta,.empty{color:var(--muted)}.sub{margin:0}.nav,.actions,.source-links,.chips{display:flex;flex-wrap:wrap;gap:8px;align-items:center}.nav{margin-top:12px}.nav a,.source-links a{border:1px solid #315264;background:#142d38;color:#b9cadc;border-radius:7px;padding:9px 13px;text-decoration:none;font-weight:700}.nav a.active{background:var(--accent);border-color:var(--accent);color:#07171b}main{padding:24px 0 60px}.panel{background:rgba(16,29,38,.96);border:1px solid var(--line);border-radius:12px;padding:18px;margin-bottom:18px;box-shadow:0 10px 35px #0003}.evidence-guide{position:sticky;top:var(--hunt-header-offset);z-index:80;max-height:calc(100vh - var(--hunt-header-offset) - 12px);overflow:auto;background:rgba(16,29,38,.985);box-shadow:0 14px 40px #0008}.overview{display:flex;justify-content:space-between;gap:18px;align-items:center}.overview p{margin:3px 0}.picker{display:grid;grid-template-columns:1.5fr 1.5fr auto auto;gap:10px;align-items:end}.filters{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;align-items:end;margin:14px 0}.checkbox{display:flex;align-items:center;gap:7px;min-height:42px}.checkbox input{width:auto}label{display:block;color:#bed0da;font-size:12px;font-weight:700;margin:4px 0 5px}select,input{width:100%;background:#0b1520;color:var(--text);border:1px solid #365268;border-radius:8px;padding:10px 11px;font:inherit}button{border:1px solid #3b6f91;background:#174665;color:#fff;border-radius:7px;padding:10px 14px;font:inherit;font-weight:750;cursor:pointer}button.secondary{background:transparent;border-color:#49617d}button:disabled{opacity:.5;cursor:not-allowed}.status{min-height:22px;margin-top:10px;color:var(--muted)}.status.good{color:var(--good)}.status.warn{color:var(--warn)}.status.bad{color:var(--bad)}.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:9px;margin:13px 0}.card,.category-card,.change-card,.evidence-card{background:#0a1720;border:1px solid var(--line);border-radius:8px;padding:11px}.card strong,.change-card strong{display:block;color:var(--blue);font-size:20px}.category-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.category-card strong{display:block;font-size:17px;color:var(--accent)}.evidence-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:10px;margin-top:13px}.evidence-card strong{display:block;margin-bottom:5px;font-size:15px}.evidence-card p{margin:0;color:var(--muted);font-size:12px}.chip{display:inline-block;margin:2px 4px 2px 0;padding:3px 7px;border-radius:99px;background:#243f4d;font-size:11px}.chip.exposed{background:#34414a}.chip.inferred{background:#4a3d21;color:#ffe3a8}.chip.observed{background:#173f5c;color:#bce4ff}.chip.correlated{background:#174b3c;color:#bdf4dc}.chip.configuration{background:#3b2c66;color:#e1d4ff}.chip.nonstandard{background:#512e54;color:#f1c8f3}.origin-note{display:inline-block;margin-top:4px;color:#e1d4ff;font-size:11px}.mac-note{display:inline-block;margin-top:3px;color:#9cd4ff;font-size:11px}.mac-note.indirect{color:#ffe3a8}.mac-note a{color:inherit}.provenance-icon{display:inline-flex;align-items:center;justify-content:center;width:16px;height:16px;margin-left:4px;border:1px solid currentColor;border-radius:50%;font-size:10px;font-weight:900}.searchsploit-results{display:grid;gap:8px;margin-top:12px}.searchsploit-result{background:#0a1720;border:1px solid var(--line);border-radius:8px;padding:10px;scroll-margin-top:var(--hunt-header-offset)}.searchsploit-result summary{cursor:pointer;font-weight:700}.searchsploit-result ul{margin:8px 0 0;padding-left:20px}.searchsploit-result a{color:var(--blue)}.searchsploit-filters{grid-template-columns:repeat(3,minmax(0,1fr))}.searchsploit-result.hidden-searchsploit-result,.searchsploit-candidate.hidden-searchsploit-candidate{display:none}.host-cve-select{min-width:250px;font-size:11px;padding:7px}.finding-match-slot{margin-top:6px}.match-badge{padding:4px 8px;border-color:#8b692a;background:#4a3d21;color:#ffe3a8;border-radius:99px;font-size:11px;white-space:nowrap}.table-wrap{overflow:auto}table{width:100%;border-collapse:collapse;min-width:980px}th,td{text-align:left;vertical-align:top;padding:8px;border-bottom:1px solid var(--line);font-size:12px}th{color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.04em}.host-row.hidden-row,.finding-row.hidden-row{display:none}.warning{padding:10px 12px;background:#2b2416;border:1px solid #8b692a;color:#ffe3a8;border-radius:8px;margin:8px 0}.changes{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:12px 0}.change-list{display:grid;grid-template-columns:1fr 1fr;gap:12px}.change-list section{background:#0a1720;border:1px solid var(--line);border-radius:9px;padding:12px}.change-list ul{margin:8px 0;padding-left:18px}.hidden{display:none!important}.footer{color:var(--muted);font-size:12px}details.panel>summary{cursor:pointer;font-size:16px;font-weight:750}details.panel[open]>summary{margin-bottom:14px}.scope-list{margin:8px 0 0;padding-left:18px;color:var(--muted)}@media(max-width:1320px){header,main{padding-left:4vw;padding-right:4vw}}@media(max-width:950px){.picker,.filters,.searchsploit-filters,.change-list{grid-template-columns:1fr}.cards,.category-grid,.changes,.evidence-grid{grid-template-columns:1fr 1fr}.overview{align-items:flex-start;flex-direction:column}}
.searchsploit-result{scroll-margin-top:var(--hunt-result-offset,420px)}
</style></head><body>
<header><div class="eyebrow">Network-wide capability review</div><h1>Hunt</h1><p class="sub">Correlate the newest retained scan evidence across the network without executing anything.</p><nav class="nav" aria-label="Primary"><a href="/scans">Nmap</a><a href="/device-config">Device</a><a href="/analysis">Analyze</a><a class="active" href="/hunting" aria-current="page">Hunt</a><a href="/network-map">Map</a></nav></header>
<main>
<section class="panel overview"><div><h2 id="huntTitle">Latest network-wide evidence</h2><p id="huntSubtitle" class="meta">Loading the newest completed scan for each network scope…</p><div id="status" class="status" role="status"></div></div><div class="actions"><button id="refreshNetwork">Refresh network view</button><a class="source-links" href="/hunting">Reset to network-wide view</a></div></section>
<section id="huntPanel" class="hidden"><section class="panel"><div id="cards" class="cards"></div><div id="sources" class="source-links"></div><div id="scopeSummary"></div><div id="warnings"></div></section>
<section class="panel evidence-guide"><h2>Capability evidence guide</h2><p class="hint">These labels describe how NCT reached a capability classification. They do not, by themselves, prove a vulnerability or compromise.</p><div class="evidence-grid"><div class="evidence-card"><strong><span class="chip exposed">Exposed</span></strong><p>Nmap reported the port as open or open/filtered. Something is reachable, but NCT may not know what application is listening.</p></div><div class="evidence-card"><strong><span class="chip inferred">Inferred</span></strong><p>The port number is commonly associated with this capability. It is a useful lead, but applications can be moved to different ports.</p></div><div class="evidence-card"><strong><span class="chip observed">Observed</span></strong><p>Nmap identified a matching service or product fingerprint. This is stronger service evidence and can also identify services on unusual ports.</p></div><div class="evidence-card"><strong><span class="chip correlated">Correlated</span></strong><p>The conventional port and the observed fingerprint agree. This is NCT's strongest capability classification, but still requires analyst validation.</p></div><div class="evidence-card"><strong><span class="chip configuration">Configuration</span></strong><p>NCT derived the capability from a retained router or firewall configuration. It is not an open-port claim and links back to the source file.</p></div></div></section>
<section class="panel"><h2>Capability datasets</h2><div id="categories" class="category-grid"></div></section>
<section class="panel"><h2>SearchSploit enrichment <span id="searchsploitBadge" class="chip">checking</span></h2><p class="hint">Compare observed product and version fingerprints with the local, offline Exploit-DB index. Potential product/version matches require analyst validation; NCT never executes exploit code.</p><div class="actions"><button id="searchsploitRun" disabled>Find potential matches</button><label class="checkbox"><input id="matchedOnly" type="checkbox" disabled> Show only systems with matches</label></div><div id="cveSummary" class="cards hidden"></div><div class="filters searchsploit-filters"><div><label for="cveFilter">CVE identifier and SearchSploit title</label><select id="cveFilter" disabled><option value="">All identified CVEs</option></select></div><div><label for="cveYearFilter">CVE year category</label><select id="cveYearFilter" disabled><option value="">All CVE years</option></select></div><div><label for="cveStatusFilter">CVE reference status</label><select id="cveStatusFilter" disabled><option value="">All candidates</option><option value="with_cve">Candidates with CVEs</option><option value="without_cve">Candidates without CVEs</option></select></div></div><p class="hint">The readable name shown with a CVE is the SearchSploit candidate title, not a separate authoritative CVE title.</p><p id="cveFilterCount" class="meta"></p><details><summary>Manage offline database</summary><p class="hint">Connected systems can download the official Exploit-DB archive. Air-gapped systems can upload the same repository as a ZIP, TAR, TAR.GZ, or TGZ package. NCT validates and stages updates before activation and keeps prior versions for rollback.</p><div class="actions"><button id="searchsploitOnline" class="secondary">Update from internet</button><input id="searchsploitUploadFile" type="file" accept=".zip,.tar,.gz,.tgz,.tar.gz"><button id="searchsploitUpload" class="secondary">Upload offline update</button></div><div class="actions"><select id="searchsploitVersions" aria-label="Previous SearchSploit database"><option value="">No previous database versions</option></select><button id="searchsploitRollback" class="secondary" disabled>Activate selected version</button></div></details><div id="searchsploitStatus" class="status"></div><div id="searchsploitResults" class="searchsploit-results"></div></section>
<section class="panel"><h2>Network filters</h2><p class="hint">Filters apply to both the host inventory and the capability findings below.</p><div class="filters"><div><label for="search">Host, service, product, version, or port</label><input id="search" type="search" placeholder="Try SSH, 443, Windows, or a hostname"></div><div><label for="osFilter">Operating system</label><select id="osFilter"><option value="">All operating systems</option></select></div><div><label for="subnetFilter">Subnet</label><select id="subnetFilter"><option value="">All subnets</option></select></div><div><label for="deviceTypeFilter">Device type</label><select id="deviceTypeFilter"><option value="">All device types</option></select></div><div><label for="category">Dataset</label><select id="category"><option value="">All datasets</option></select></div><div><label for="capability">Capability evidence</label><select id="capability"><option value="">All evidence levels</option><option value="exposed">Exposed</option><option value="inferred">Inferred</option><option value="observed">Observed</option><option value="correlated">Correlated</option><option value="configuration">Configuration</option></select></div><div><label for="protocol">Evidence source</label><select id="protocol"><option value="">All evidence sources</option><option value="tcp">TCP</option><option value="udp">UDP</option><option value="configuration">Device configuration</option></select></div><label class="checkbox"><input id="nonstandard" type="checkbox"> Nonstandard ports only</label></div><p id="visibleCount" class="meta"></p></section>
<details class="panel" id="inventoryPanel"><summary id="inventorySummary">Host and device inventory</summary><p class="hint">Nmap hosts and retained router/firewall configurations are shown together. Configuration-only devices are clearly labeled and are not treated as Nmap observations.</p><div class="table-wrap"><table><thead><tr><th>Host / device</th><th>Subnet</th><th>Operating system</th><th>Device type</th><th>Capabilities</th><th>SearchSploit CVEs / common names</th><th>Latest evidence</th></tr></thead><tbody id="hostRows"></tbody></table></div></details>
<section class="panel"><h2>Capability findings</h2><div class="table-wrap"><table><thead><tr><th>Host</th><th>Subnet / device type</th><th>Dataset</th><th>Capability evidence</th><th>Port / source</th><th>Observed evidence</th><th>Match basis</th></tr></thead><tbody id="findingRows"></tbody></table></div></section></section>
<details class="panel"><summary>Focus on one scan or compare two scans</summary><p class="hint">The default view above is network-wide. Use this section when you need to isolate one retained scan or compare it with an older scan.</p><div class="picker"><div><label for="currentRun">Scan to review</label><select id="currentRun"><option value="">Loading completed scans…</option></select></div><div><label for="baselineRun">Optional comparison baseline</label><select id="baselineRun"><option value="">No comparison</option></select></div><button id="hunt">Open scan</button><button id="compare" class="secondary" disabled>Compare</button></div></details>
<section id="comparisonPanel" class="panel hidden"><h2>Hunting changes</h2><div id="comparison"></div></section><div class="footer">Build <code>{APP_VERSION}</code> · <code>{BUILD_ID}</code></div></main>
<script>
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let candidates=[],current=null,matchedHostKeys=null,searchsploitMatches=new Map(),searchsploitData=null,cveFilteredHostKeys=null,cveFilteredMatchKeys=null;
const pageHeader=document.querySelector('header'),evidenceGuide=document.querySelector('.evidence-guide');function updateStickyOffset(){const headerOffset=Math.ceil(pageHeader.getBoundingClientRect().height)+12,guideHeight=Math.ceil(evidenceGuide.getBoundingClientRect().height);document.documentElement.style.setProperty('--hunt-header-offset',`${headerOffset}px`);document.documentElement.style.setProperty('--hunt-result-offset',`${headerOffset+guideHeight+14}px`)}updateStickyOffset();const stickyOffsetObserver=new ResizeObserver(updateStickyOffset);stickyOffsetObserver.observe(pageHeader);stickyOffsetObserver.observe(evidenceGuide);window.addEventListener('resize',updateStickyOffset);
function setStatus(message,kind=''){$('status').textContent=message;$('status').className='status '+kind}
function when(value){if(!value)return'—';const parsed=new Date(value);return Number.isNaN(parsed.valueOf())?value:parsed.toLocaleString()}
function label(item){return item.comparison_name||`${item.display_name||'Scan'} · ${when(item.completed_at||item.created_at)}`}
function optionRows(items){return items.map(item=>`<option value="${esc(item.selection_run_id)}">${esc(label(item))}</option>`).join('')}
function refreshBaselines(){const id=$('currentRun').value;$('baselineRun').innerHTML='<option value="">No comparison</option>'+optionRows(candidates.filter(item=>item.selection_run_id!==id));$('compare').disabled=!id||!$('baselineRun').value}
function stateChips(item){return(item.evidence_states||[]).map(state=>`<span class="chip ${esc(state)}">${esc(state)}</span>`).join('')+(item.nonstandard_port?'<span class="chip nonstandard">nonstandard port</span>':'')}
function sourceLinks(source){return(source?.evidence?.sources||[]).map(item=>`<a href="${esc(item.url)}" target="_blank" rel="noopener">${esc(item.label)}</a>`).join('')}
function macEvidence(item){if(!item.mac)return'<span class="meta">MAC not observed</span>';const p=item.mac_provenance||{},title=[p.origin,p.source_label,p.timestamp,p.detail].filter(Boolean).join(' · '),link=p.source_url?` · <a href="${esc(p.source_url)}" target="_blank" rel="noopener">source</a>`:'';return`<code>${esc(item.mac)}</code><br><span class="mac-note${p.indirect?' indirect':''}" title="${esc(title)}"><span class="provenance-icon">i</span> ${esc(p.indirect?'correlated by IP':'direct Nmap')}${link}</span>`}
function originEvidence(item){if(item.evidence_origin==='configuration_only')return'<br><span class="origin-note"><span class="provenance-icon">i</span> configuration only · not observed by Nmap</span>';if(item.evidence_origin==='nmap_and_configuration')return'<br><span class="origin-note"><span class="provenance-icon">i</span> Nmap + device configuration</span>';return''}
function findingTransport(item){return item.evidence_kind==='device_configuration'||!item.port?'Device configuration':`${esc(item.port)}/${esc(item.protocol)}`}
function findingMatchKey(item){return[item.host_key,item.protocol,item.port,item.product,item.version].map(value=>String(value??'').trim().toLowerCase()).join('|')}
function openSearchSploitMatch(key){const target=[...document.querySelectorAll('.searchsploit-result')].find(item=>item.dataset.matchkey===key);if(!target)return;target.open=true;target.scrollIntoView({behavior:'smooth',block:'start'})}
function renderFindingMatchBadges(){document.querySelectorAll('.finding-match-slot').forEach(slot=>{const match=searchsploitMatches.get(slot.dataset.matchkey);slot.innerHTML='';if(!match)return;const button=document.createElement('button');button.type='button';button.className='match-badge';button.textContent=`Potential matches: ${Number(match.candidate_count||0)}`;button.title='Open SearchSploit details for this product and version';button.onclick=()=>openSearchSploitMatch(slot.dataset.matchkey);slot.append(button)})}
function facetOptions(items,placeholder){return`<option value="">${esc(placeholder)}</option>`+(items||[]).map(item=>`<option value="${esc(item.name)}">${esc(item.name)} (${Number(item.host_count||0)})</option>`).join('')}
function rowData(item){const services=(item.services||[]).flatMap(value=>[value.port,value.protocol,value.service,value.product,value.version]),search=[item.ip,item.hostname,item.mac,item.vendor,item.os,item.os_group,item.os_filter,item.subnet,item.device_type,item.category,item.protocol,item.port,item.service,item.product,item.version,...services,...(item.categories||[]),...(item.match_basis||[])].join(' ').toLowerCase();return`data-hostkey="${esc(item.host_key||'')}" data-matchkey="${esc(findingMatchKey(item))}" data-search="${esc(search)}" data-os="${esc(item.os_filter||'Unclassified')}" data-subnet="${esc(item.subnet||'Unmapped')}" data-device="${esc(item.device_type||'Unclassified')}" data-category="${esc(item.category||(item.categories||[]).join('|'))}" data-protocol="${esc(item.protocol||(item.protocols||[]).join(' '))}" data-states="${esc((item.evidence_states||item.capability_states||[]).join(' '))}" data-nonstandard="${item.nonstandard_port?'yes':'no'}"`}
function renderHunt(data){current=data;matchedHostKeys=null;searchsploitMatches=new Map();$('matchedOnly').checked=false;$('matchedOnly').disabled=true;$('searchsploitResults').innerHTML='';$('huntPanel').classList.remove('hidden');const source=data.source||{},states=data.capability_states||{},facets=data.facets||{},datasets=data.datasets||data.categories||[];$('huntTitle').textContent=source.display_name||'Hunt';$('huntSubtitle').textContent=source.comparison_name||when(source.completed_at||source.created_at);$('inventorySummary').textContent=`Host and device inventory · ${Number(data.host_count||0)}`;const cards=[['Nmap hosts',data.nmap_host_count],['Configuration devices',data.configuration_device_count],['Hosts / devices with findings',data.hosts_with_findings_count],['Capability findings',data.finding_count],['Observed fingerprints',states.observed],['Correlated findings',states.correlated]];$('cards').innerHTML=cards.map(([name,value])=>`<div class="card"><strong>${Number(value||0)}</strong>${esc(name)}</div>`).join('');$('sources').innerHTML=sourceLinks(source);$('scopeSummary').innerHTML=(source.scope_summaries||[]).length?`<details><summary>Newest evidence selected from ${source.scope_summaries.length} network scope${source.scope_summaries.length===1?'':'s'}</summary><ul class="scope-list">${source.scope_summaries.map(item=>`<li>${esc(item.display_name||'Scan')} · ${esc((item.subnets||[]).join(', ')||'Unmapped scope')} · ${esc(when(item.completed_at))}</li>`).join('')}</ul></details>`:'';$('warnings').innerHTML=(data.warnings||[]).map(item=>`<div class="warning">${esc(item)}</div>`).join('');$('categories').innerHTML=datasets.map(item=>`<div class="category-card"><strong>${item.finding_count}</strong>${esc(item.name)}</div>`).join('')||'<p class="empty">No matching datasets were found.</p>';$('category').innerHTML='<option value="">All datasets</option>'+datasets.map(item=>`<option value="${esc(item.name)}">${esc(item.name)}</option>`).join('');$('osFilter').innerHTML=facetOptions(facets.operating_systems,'All operating systems');$('subnetFilter').innerHTML=facetOptions(facets.subnets,'All subnets');$('deviceTypeFilter').innerHTML=facetOptions(facets.device_types,'All device types');$('hostRows').innerHTML=(data.hosts||[]).map(item=>`<tr class="host-row" ${rowData(item)}><td><strong>${esc(item.ip||item.hostname||'—')}</strong><br><span class="meta">${esc(item.hostname||'')}</span>${originEvidence(item)}<br>${macEvidence(item)}</td><td><code>${esc(item.subnet||'Unmapped')}</code></td><td>${esc(item.os||item.os_group||'Unclassified')}<br><span class="meta">${esc(item.vendor||'')}</span></td><td>${esc(item.device_type||'Unclassified')}</td><td>${(item.categories||[]).map(value=>`<span class="chip">${esc(value)}</span>`).join('')||'<span class="meta">No matching capability evidence</span>'}</td><td>${esc(when(item.last_observed))}<br>${sourceLinks({evidence:{sources:item.source_refs||[]}})}</td></tr>`).join('');$('findingRows').innerHTML=(data.findings||[]).map(item=>`<tr class="finding-row" ${rowData(item)}><td><strong>${esc(item.ip||item.hostname||'—')}</strong><br><span class="meta">${esc(item.hostname||item.os_group||'')}</span>${originEvidence(item)}<br>${macEvidence(item)}</td><td><code>${esc(item.subnet||'Unmapped')}</code><br><span class="meta">${esc(item.device_type||'Unclassified')}</span></td><td>${esc(item.category)}</td><td>${stateChips(item)}</td><td><code>${findingTransport(item)}</code>${item.outlier?'<br><span class="chip inferred">outlier</span>':''}</td><td>${esc(item.service||'unknown')}<br><span class="meta">${esc([item.product,item.version].filter(Boolean).join(' ')||(item.evidence_kind==='device_configuration'?'Retained configuration evidence':'No product fingerprint'))}</span><div class="finding-match-slot" data-matchkey="${esc(findingMatchKey(item))}"></div></td><td>${(item.match_basis||[]).map(esc).join('<br>')}</td></tr>`).join('');applyFilters()}
const renderHuntBase=renderHunt;renderHunt=function(data){renderHuntBase(data);searchsploitData=null;cveFilteredHostKeys=null;cveFilteredMatchKeys=null;for(const id of['cveFilter','cveYearFilter','cveStatusFilter']){$(id).value='';$(id).disabled=true}$('cveSummary').classList.add('hidden');$('cveFilterCount').textContent='';document.querySelectorAll('.host-row').forEach(row=>{const cell=document.createElement('td'),slot=document.createElement('div');slot.className='host-cve-slot';slot.dataset.hostkey=row.dataset.hostkey;slot.innerHTML='<span class="meta">Run SearchSploit to identify CVEs</span>';cell.append(slot);row.insertBefore(cell,row.lastElementChild)})}
function matches(row,isHost){const term=$('search').value.trim().toLowerCase(),os=$('osFilter').value,subnet=$('subnetFilter').value,device=$('deviceTypeFilter').value,category=$('category').value,protocol=$('protocol').value,state=$('capability').value,nonstandard=$('nonstandard').checked,matchedOnly=$('matchedOnly').checked,cveFilterActive=$('cveFilter').value||$('cveYearFilter').value||$('cveStatusFilter').value;const categoryMatch=!category||(isHost?row.dataset.category.split('|').includes(category):row.dataset.category===category),protocolMatch=!protocol||(isHost?row.dataset.protocol.split(' ').includes(protocol):row.dataset.protocol===protocol),searchsploitMatch=!matchedOnly||(matchedHostKeys&&matchedHostKeys.has(row.dataset.hostkey)),cveMatch=!cveFilterActive||(isHost?cveFilteredHostKeys&&cveFilteredHostKeys.has(row.dataset.hostkey):cveFilteredMatchKeys&&cveFilteredMatchKeys.has(row.dataset.matchkey));return(!term||row.dataset.search.includes(term))&&(!os||row.dataset.os===os)&&(!subnet||row.dataset.subnet===subnet)&&(!device||row.dataset.device===device)&&categoryMatch&&protocolMatch&&(!state||row.dataset.states.split(' ').includes(state))&&(!nonstandard||row.dataset.nonstandard==='yes')&&searchsploitMatch&&cveMatch}
function applyFilters(){if(!current)return;let hosts=0,findings=0;document.querySelectorAll('.host-row').forEach(row=>{const show=matches(row,true);row.classList.toggle('hidden-row',!show);if(show)hosts++});document.querySelectorAll('.finding-row').forEach(row=>{const show=matches(row,false);row.classList.toggle('hidden-row',!show);if(show)findings++});$('visibleCount').textContent=`${hosts} of ${current.host_count||0} hosts / devices and ${findings} of ${current.finding_count||0} capability findings shown.`}
async function loadNetwork(){setStatus('Correlating the newest retained scans across the network…');try{const response=await fetch('/api/hunting/network'),data=await response.json();if(!response.ok)throw new Error(data.detail||'Network hunting view failed');renderHunt(data);setStatus('Network-wide hunting view is current.','good');history.replaceState(null,'','/hunting')}catch(error){setStatus(error.message,'bad')}}
async function hunt(){const id=$('currentRun').value;if(!id){setStatus('Choose a completed scan first.','warn');return}setStatus('Opening the selected scan…');try{const response=await fetch(`/api/hunting/${encodeURIComponent(id)}`),data=await response.json();if(!response.ok)throw new Error(data.detail||'Hunting analysis failed');renderHunt(data);setStatus('Focused scan view is current.','good');history.replaceState(null,'',`/hunting?run=${encodeURIComponent(id)}`)}catch(error){setStatus(error.message,'bad')}}
function findingText(item){return`${item.ip||item.hostname||'host'} · ${item.category} · ${item.evidence_kind==='device_configuration'?'device configuration':`${item.port}/${item.protocol}`} ${item.service||''}`}
function list(title,items,render){return`<section><h3>${esc(title)} <span class="chip">${items.length}</span></h3>${items.length?`<ul>${items.map(item=>`<li>${esc(render(item))}</li>`).join('')}</ul>`:'<p class="empty">None</p>'}</section>`}
async function compare(){const before=$('baselineRun').value,after=$('currentRun').value;if(!before||!after)return;setStatus('Comparing hunting evidence…');try{const response=await fetch(`/api/hunting/compare?before=${encodeURIComponent(before)}&after=${encodeURIComponent(after)}`),data=await response.json();if(!response.ok)throw new Error(data.detail||'Hunting comparison failed');const s=data.summary||{};$('comparisonPanel').classList.remove('hidden');const coverage=(data.coverage_warnings||[]).map(item=>`<div class="warning">${esc(item)}</div>`).join('');$('comparison').innerHTML=`${coverage}<p class="meta">${esc(data.before?.comparison_name||'Baseline')} → ${esc(data.after?.comparison_name||'Current')}</p><div class="changes">${Object.entries(s).map(([name,value])=>`<div class="change-card"><strong>${value}</strong>${esc(name.replaceAll('_',' '))}</div>`).join('')}</div><div class="change-list">${list('Findings added',data.findings_added||[],findingText)}${list('Findings removed',data.findings_removed||[],findingText)}${list('Findings changed',data.findings_changed||[],item=>`${findingText(item.finding)} · ${Object.keys(item.changes).join(', ')}`)}${list('Host dataset changes',data.host_category_changes||[],item=>`${item.host_key} · added: ${item.categories_added.join(', ')||'—'} · removed: ${item.categories_removed.join(', ')||'—'}`)}</div>`;setStatus('Hunting comparison complete.','good');$('comparisonPanel').scrollIntoView({behavior:'smooth',block:'start'})}catch(error){setStatus(error.message,'bad')}}
async function loadCandidates(){try{const response=await fetch('/api/scan-comparisons/candidates'),data=await response.json();if(!response.ok)throw new Error(data.detail||'Completed scans unavailable');candidates=data;$('currentRun').innerHTML='<option value="">Choose a completed scan…</option>'+optionRows(candidates);const requested=new URLSearchParams(location.search).get('run');if(requested&&candidates.some(item=>item.selection_run_id===requested))$('currentRun').value=requested;refreshBaselines()}catch(error){setStatus(error.message,'bad')}}
function renderSearchSploitDatabase(data){$('searchsploitBadge').textContent=data.available?'offline database ready':'not installed';$('searchsploitBadge').className=`chip ${data.available?'correlated':'inferred'}`;const installed=data.installed_at?` Installed ${when(data.installed_at)}.`:'',source=data.source?` Source: ${data.source}.`:'';$('searchsploitStatus').textContent=`${data.message||''}${installed}${source}`;$('searchsploitStatus').className=`status ${data.available?'good':'warn'}`;$('searchsploitRun').disabled=!data.available;const previous=(data.versions||[]).filter(item=>!item.active);$('searchsploitVersions').innerHTML='<option value="">'+(previous.length?'Choose a previous version…':'No previous database versions')+'</option>'+previous.map(item=>`<option value="${esc(item.version_id)}">${esc(item.version_id)} · ${esc(when(item.installed_at))}</option>`).join('');$('searchsploitRollback').disabled=!$('searchsploitVersions').value}
async function loadSearchSploitStatus(){try{const response=await fetch('/api/searchsploit/status'),data=await response.json();if(!response.ok)throw new Error(data.detail||'SearchSploit status unavailable');renderSearchSploitDatabase(data)}catch(error){$('searchsploitStatus').textContent=error.message;$('searchsploitStatus').className='status bad'}}
function renderSearchSploit(data){const results=data.matches||[],queryCount=Number(data.query_count||0);matchedHostKeys=new Set(results.map(item=>String(item.host_key||'')));searchsploitMatches=new Map(results.map(item=>[item.match_key||findingMatchKey(item),item]));$('matchedOnly').disabled=false;if(queryCount===0){$('searchsploitStatus').textContent=`The database is ready, but this evidence contains no specific product/version fingerprints to search. Run an Nmap scan with Service/version detection enabled, then try again. ${data.disclaimer||''}`;$('searchsploitStatus').className='status warn'}else{$('searchsploitStatus').textContent=`${queryCount} product/version queries · ${Number(data.matched_host_count||0)} matched systems · ${Number(data.match_count||0)} potential matches. ${data.disclaimer||''}`;$('searchsploitStatus').className='status '+((data.warnings||[]).length?'warn':'good')}$('searchsploitResults').innerHTML=results.map(item=>`<details class="searchsploit-result" data-matchkey="${esc(item.match_key||findingMatchKey(item))}"><summary>${esc(item.ip||item.hostname||'Host')} · ${esc(item.port)}/${esc(item.protocol)} · ${esc(item.product||item.service||'service')} ${esc(item.version||'')} · ${Number(item.candidate_count||0)} candidate${Number(item.candidate_count||0)===1?'':'s'}</summary><ul>${(item.candidates||[]).map(candidate=>`<li><a href="${esc(candidate.url||'#')}" target="_blank" rel="noopener">EDB-${esc(candidate.edb_id||'—')}</a> · ${esc(candidate.title||'Untitled')} · ${esc(candidate.platform||'unknown platform')} · ${esc(candidate.type||'unknown type')}${candidate.codes?` · ${esc(candidate.codes)}`:''}</li>`).join('')}</ul></details>`).join('')||(queryCount===0?'<p class="empty">No searches were run because no product/version fingerprints were available.</p>':'<p class="empty">The product/version searches completed, but no potential matches were returned.</p>');renderFindingMatchBadges();applyFilters()}
function candidateCves(candidate){return[...new Set([...(candidate.cves||[]),...((String(candidate.codes||'').match(/CVE-\d{4}-\d{4,}/gi))||[])].map(value=>String(value).toUpperCase()))]}
function configureCveFilters(data){const facets=data.cve_facets||[],years=new Map();for(const item of facets)years.set(String(item.year||''),(years.get(String(item.year||''))||0)+Number(item.candidate_count||0));$('cveFilter').innerHTML='<option value="">All identified CVEs</option>'+facets.map(item=>{const names=item.common_names||[],name=names[0]?` · ${names[0]}${names.length>1?` (+${names.length-1} titles)`:''}`:'';return`<option value="${esc(item.cve)}">${esc(item.cve+name)} · ${Number(item.matched_host_count||0)} host${Number(item.matched_host_count||0)===1?'':'s'}</option>`}).join('');$('cveYearFilter').innerHTML='<option value="">All CVE years</option>'+[...years.entries()].sort((a,b)=>b[0].localeCompare(a[0])).map(([year,count])=>`<option value="${esc(year)}">${esc(year)} (${count} candidate entries)</option>`).join('');for(const id of['cveFilter','cveYearFilter','cveStatusFilter'])$(id).disabled=!(data.matches||[]).length;$('cveSummary').innerHTML=`<div class="card"><strong>${Number(data.cve_count||0)}</strong>unique CVEs</div><div class="card"><strong>${Number(data.cve_candidate_count||0)}</strong>CVE-linked candidate entries</div><div class="card"><strong>${Number(data.non_cve_candidate_count||0)}</strong>candidate entries without CVEs</div><div class="card"><strong>${years.size}</strong>CVE year categories</div>`;$('cveSummary').classList.toggle('hidden',!(data.matches||[]).length)}
function renderHostCveDropdowns(data){const byHost=new Map();for(const match of data.matches||[]){const host=String(match.host_key||'');if(!host)continue;const entries=byHost.get(host)||new Map();for(const candidate of match.candidates||[])for(const cve of candidateCves(candidate)){const names=entries.get(cve)||new Set(),title=String(candidate.title||'').trim();if(title)names.add(title);entries.set(cve,names)}byHost.set(host,entries)}document.querySelectorAll('.host-cve-slot').forEach(slot=>{const entries=byHost.get(slot.dataset.hostkey);if(!entries||!entries.size){slot.innerHTML='<span class="meta">No CVE-linked candidates</span>';return}const select=document.createElement('select');select.className='host-cve-select';select.setAttribute('aria-label','SearchSploit CVEs and candidate titles');select.innerHTML=`<option value="">${entries.size} CVE${entries.size===1?'':'s'} · choose to filter</option>`+[...entries.entries()].sort((a,b)=>b[0].localeCompare(a[0])).map(([cve,names])=>{const titles=[...names],label=titles[0]?`${cve} · ${titles[0]}${titles.length>1?` (+${titles.length-1} titles)`:''}`:cve;return`<option value="${esc(cve)}">${esc(label)}</option>`}).join('');select.onchange=()=>{if(!select.value)return;$('cveFilter').value=select.value;$('cveYearFilter').value='';$('cveStatusFilter').value='';applySearchSploitFilters();const target=[...document.querySelectorAll('.searchsploit-result:not(.hidden-searchsploit-result)')][0];if(target){target.open=true;target.scrollIntoView({behavior:'smooth',block:'start'})}};slot.replaceChildren(select)})}
function applySearchSploitFilters(){if(!searchsploitData)return;const cve=$('cveFilter').value,year=$('cveYearFilter').value,statusFilter=$('cveStatusFilter').value,active=Boolean(cve||year||statusFilter),hosts=new Set(),matchKeys=new Set();let visibleCandidates=0,visibleMatches=0;document.querySelectorAll('.searchsploit-result').forEach(detail=>{let detailVisible=0;detail.querySelectorAll('.searchsploit-candidate').forEach(item=>{const cves=(item.dataset.cves||'').split(' ').filter(Boolean),years=(item.dataset.cveYears||'').split(' ').filter(Boolean),hasCve=cves.length>0,show=(!cve||cves.includes(cve))&&(!year||years.includes(year))&&(!statusFilter||(statusFilter==='with_cve'?hasCve:!hasCve));item.classList.toggle('hidden-searchsploit-candidate',!show);if(show){detailVisible++;visibleCandidates++}});detail.classList.toggle('hidden-searchsploit-result',!detailVisible);if(detailVisible){visibleMatches++;hosts.add(detail.dataset.hostkey);matchKeys.add(detail.dataset.matchkey)}});cveFilteredHostKeys=active?hosts:null;cveFilteredMatchKeys=active?matchKeys:null;$('cveFilterCount').textContent=`${visibleCandidates} of ${Number(searchsploitData.match_count||0)} candidate entries across ${visibleMatches} product/service matches shown.`;applyFilters()}
const renderSearchSploitBase=renderSearchSploit;renderSearchSploit=function(data){renderSearchSploitBase(data);searchsploitData=data;configureCveFilters(data);document.querySelectorAll('.searchsploit-result').forEach((detail,index)=>{const match=(data.matches||[])[index]||{};detail.dataset.hostkey=String(match.host_key||'');detail.querySelectorAll('li').forEach((item,candidateIndex)=>{const candidate=(match.candidates||[])[candidateIndex]||{},cves=candidateCves(candidate);item.classList.add('searchsploit-candidate');item.dataset.cves=cves.join(' ');item.dataset.cveYears=[...new Set(cves.map(value=>value.split('-')[1]))].join(' ')})});renderHostCveDropdowns(data);applySearchSploitFilters()}
async function runSearchSploit(){const run=new URLSearchParams(location.search).get('run'),endpoint=run?`/api/searchsploit/hunting/${encodeURIComponent(run)}`:'/api/searchsploit/hunting/network';$('searchsploitRun').disabled=true;$('searchsploitStatus').textContent='Searching the local Exploit-DB index…';try{const response=await fetch(endpoint,{method:'POST'}),data=await response.json();if(!response.ok)throw new Error(data.detail||'SearchSploit enrichment failed');renderSearchSploit(data)}catch(error){$('searchsploitStatus').textContent=error.message;$('searchsploitStatus').className='status bad'}finally{$('searchsploitRun').disabled=false}}
async function updateSearchSploitOnline(){if(!confirm('Download, validate, and activate the latest official Exploit-DB database? The current version will remain available for rollback.'))return;$('searchsploitOnline').disabled=true;$('searchsploitStatus').textContent='Downloading and validating the official Exploit-DB update…';try{const response=await fetch('/api/searchsploit/database/update-online',{method:'POST'}),data=await response.json();if(!response.ok)throw new Error(data.detail||'Online database update failed');renderSearchSploitDatabase(data)}catch(error){$('searchsploitStatus').textContent=error.message;$('searchsploitStatus').className='status bad'}finally{$('searchsploitOnline').disabled=false}}
async function uploadSearchSploit(){const file=$('searchsploitUploadFile').files[0];if(!file){$('searchsploitStatus').textContent='Choose an Exploit-DB update package first.';$('searchsploitStatus').className='status warn';return}if(!confirm(`Validate and activate ${file.name}? The current database will remain available for rollback.`))return;const body=new FormData();body.append('file',file);$('searchsploitUpload').disabled=true;$('searchsploitStatus').textContent='Uploading, validating, and staging the offline database…';try{const response=await fetch('/api/searchsploit/database/upload',{method:'POST',body}),data=await response.json();if(!response.ok)throw new Error(data.detail||'Offline database upload failed');$('searchsploitUploadFile').value='';renderSearchSploitDatabase(data)}catch(error){$('searchsploitStatus').textContent=error.message;$('searchsploitStatus').className='status bad'}finally{$('searchsploitUpload').disabled=false}}
async function rollbackSearchSploit(){const version=$('searchsploitVersions').value;if(!version)return;if(!confirm(`Activate SearchSploit database ${version}?`))return;$('searchsploitRollback').disabled=true;try{const response=await fetch(`/api/searchsploit/database/rollback/${encodeURIComponent(version)}`,{method:'POST'}),data=await response.json();if(!response.ok)throw new Error(data.detail||'Database rollback failed');renderSearchSploitDatabase(data)}catch(error){$('searchsploitStatus').textContent=error.message;$('searchsploitStatus').className='status bad'}}
async function start(){const requested=new URLSearchParams(location.search).get('run');loadCandidates();loadSearchSploitStatus();if(requested){await fetch(`/api/hunting/${encodeURIComponent(requested)}`).then(async response=>{const data=await response.json();if(!response.ok)throw new Error(data.detail||'Hunting analysis failed');renderHunt(data);setStatus('Focused scan view is current.','good')}).catch(error=>setStatus(error.message,'bad'))}else await loadNetwork()}
$('currentRun').onchange=refreshBaselines;$('baselineRun').onchange=()=>{$('compare').disabled=!$('baselineRun').value||!$('currentRun').value};$('hunt').onclick=hunt;$('compare').onclick=compare;$('refreshNetwork').onclick=loadNetwork;$('searchsploitRun').onclick=runSearchSploit;$('matchedOnly').onchange=applyFilters;$('searchsploitOnline').onclick=updateSearchSploitOnline;$('searchsploitUpload').onclick=uploadSearchSploit;$('searchsploitVersions').onchange=()=>{$('searchsploitRollback').disabled=!$('searchsploitVersions').value};$('searchsploitRollback').onclick=rollbackSearchSploit;for(const id of['search','osFilter','subnetFilter','deviceTypeFilter','category','protocol','capability','nonstandard'])$(id).addEventListener(id==='search'?'input':'change',applyFilters);start();
for(const id of['cveFilter','cveYearFilter','cveStatusFilter'])$(id).addEventListener('change',applySearchSploitFilters);
</script></body></html>'''
    return HTMLResponse(
        html.replace("{APP_VERSION}", APP_VERSION).replace("{BUILD_ID}", BUILD_ID)
    )
