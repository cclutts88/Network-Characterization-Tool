from __future__ import annotations

from fastapi.responses import HTMLResponse


def device_config_page() -> HTMLResponse:
    return HTMLResponse(
        r'''<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>NCT · Device</title>
  <style>
    :root { color-scheme:dark; --bg:#08141d; --panel:#0f1d27; --line:#243b47; --text:#e7eef8; --muted:#9eb0b8; --accent:#57d6bf; --good:#61d095; --warn:#f4c95d; --bad:#ff7b7b; }
    * { box-sizing:border-box; } body { margin:0; background:linear-gradient(135deg,#08141d,#10242e); color:var(--text); font:14px/1.45 system-ui,-apple-system,Segoe UI,sans-serif; }
    header,main { max-width:1044px; margin:0 auto; } header { padding:26px 0 20px; border-bottom:1px solid var(--line); } h1 { margin:0 0 4px; font-size:27px; } h2 { margin:0 0 13px; font-size:18px; } .sub { color:var(--muted); margin:0; } .eyebrow { color:var(--accent); font-weight:700; letter-spacing:.08em; text-transform:uppercase; font-size:11px; }
    .nav { display:flex; flex-wrap:wrap; gap:8px; margin-top:18px; } .nav a { display:inline-flex; border:1px solid #315264; background:#142d38; color:#b9cadc; border-radius:7px; padding:9px 13px; text-decoration:none; font-weight:700; } .nav a:hover { background:#1b4b57; color:#fff; } .nav a.active { background:#57d6bf; border-color:#57d6bf; color:#07171b; }
    main { padding:24px 0 60px; } .grid { display:grid; grid-template-columns:minmax(0,1fr) minmax(360px,1fr); gap:18px; align-items:start; } .panel { background:rgba(15,29,39,.94); border:1px solid var(--line); border-radius:12px; padding:18px; box-shadow:0 10px 35px rgba(0,0,0,.18); } .wide { grid-column:1/-1; }
    label { display:block; color:var(--muted); font-size:12px; font-weight:650; margin:11px 0 5px; } input,select,textarea { width:100%; background:#0b161c; color:var(--text); border:1px solid #315264; border-radius:7px; padding:9px 10px; font:inherit; } textarea { min-height:150px; resize:vertical; } input:focus,select:focus,textarea:focus { outline:2px solid rgba(87,214,191,.35); border-color:var(--accent); } .two { display:grid; grid-template-columns:1fr 1fr; gap:12px; } .three { display:grid; grid-template-columns:1fr 1fr 1fr; gap:12px; }
    .actions { display:flex; flex-wrap:wrap; gap:9px; margin-top:17px; } button { border:1px solid #315264; background:#155064; color:#fff; border-radius:7px; padding:9px 13px; font:inherit; font-weight:700; cursor:pointer; } button:hover { background:#1b6870; } button.secondary { background:transparent; border-color:#49617d; } button:disabled { opacity:.55; cursor:not-allowed; } .hint,.meta { color:var(--muted); font-size:12px; } .hint { margin:7px 0 0; } #notice { min-height:22px; color:var(--muted); } #notice.good { color:var(--good); } #notice.bad { color:var(--bad); } #notice.warn { color:var(--warn); } pre { white-space:pre-wrap; overflow:auto; max-height:310px; background:#091318; border:1px solid #263f49; border-radius:7px; padding:10px; color:#cce7f5; font:12px/1.45 ui-monospace,SFMono-Regular,Consolas,monospace; } .execution-plan { display:grid; gap:9px; margin-top:9px; } .execution-step { border:1px solid #294552; border-radius:9px; background:#0b1820; padding:10px 11px; } .execution-step-head { display:flex; flex-wrap:wrap; align-items:center; gap:7px; margin-bottom:7px; } .execution-number { display:inline-grid; place-items:center; min-width:24px; height:24px; border-radius:999px; background:var(--accent); color:#07171b; font-weight:850; } .execution-phase { color:#e7f7ff; font-weight:750; } .execution-location,.execution-kind { border:1px solid #315264; border-radius:999px; padding:2px 7px; color:#b9cadc; font-size:10px; letter-spacing:.03em; text-transform:uppercase; } .execution-step pre { margin:0; max-height:190px; } .execution-detail { margin:6px 1px 0; color:var(--muted); font-size:11px; } .footer { margin-top:18px; color:var(--muted); font-size:12px; } .hidden { display:none; }
    .section-head { display:flex; align-items:flex-start; justify-content:space-between; gap:16px; } .section-head button { flex:0 0 auto; } .history-tools { display:grid; grid-template-columns:minmax(220px,1fr) minmax(180px,240px); gap:10px; margin-top:14px; } .history-list { display:grid; gap:10px; margin-top:14px; } details.device-history,details.run-card { border:1px solid #294552; border-radius:9px; background:#0b1820; } details.device-history>summary,details.run-card>summary { cursor:pointer; list-style:none; } details.device-history>summary::-webkit-details-marker,details.run-card>summary::-webkit-details-marker { display:none; } .device-summary { display:flex; align-items:center; justify-content:space-between; gap:14px; padding:13px 15px; } .device-address { color:#dff7ff; font:700 15px ui-monospace,SFMono-Regular,Consolas,monospace; } .history-latest { color:var(--muted); font-size:12px; text-align:right; } .run-count,.count-badge { display:inline-block; margin-left:8px; color:#07171b; background:var(--accent); border-radius:999px; padding:2px 7px; font:700 11px system-ui,sans-serif; } .preset { margin-left:10px; padding:5px 9px; font-size:11px; } .run-list { display:grid; gap:8px; padding:0 12px 12px; } .run-card { padding:0; } .run-summary { display:flex; justify-content:space-between; gap:12px; padding:10px 12px; color:#cce7f5; } .run-body { padding:0 12px 12px; border-top:1px solid #233b46; } .run-grid { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:8px 16px; margin-top:10px; } .run-field { color:var(--muted); font-size:12px; } .run-field strong { display:block; color:var(--text); font-weight:650; overflow-wrap:anywhere; } .artifacts { display:flex; flex-wrap:wrap; gap:8px; margin-top:10px; } .artifacts a { border:1px solid #315264; border-radius:6px; padding:6px 9px; color:#bfe8f4; text-decoration:none; font-size:12px; } .artifacts a:hover { background:#173744; color:#fff; } .candidate-detail { margin-top:10px; padding:11px 12px; border:1px solid #294552; border-radius:8px; background:#0b1820; color:#cce7f5; min-height:42px; } .danger { background:#722f38; border-color:#b95865; } .danger:hover { background:#8b3945; } .run-actions { display:flex; flex-wrap:wrap; gap:8px; margin-top:12px; } .structured-results { display:grid; gap:8px; margin-top:12px; } .result-section { border:1px solid #294552; border-radius:8px; background:#09151c; } .result-section>summary { cursor:pointer; padding:9px 11px; color:#dff7ff; font-weight:700; } .result-content { padding:0 11px 11px; overflow:auto; } .result-table { width:100%; border-collapse:collapse; min-width:560px; font-size:12px; } .result-table th,.result-table td { padding:7px 8px; border-bottom:1px solid #203842; text-align:left; vertical-align:top; overflow-wrap:anywhere; } .result-table th { position:sticky; top:0; background:#10232c; color:var(--muted); } .result-empty { color:var(--muted); padding:3px 0; } .result-note { color:var(--muted); font-size:11px; margin:0 0 7px; } .result-content pre { max-height:420px; margin:0; } #deleteChallengeCode { color:var(--warn); font:800 18px ui-monospace,SFMono-Regular,Consolas,monospace; letter-spacing:.08em; }
    dialog { width:min(470px,calc(100vw - 28px)); border:1px solid #3d6575; border-radius:12px; padding:0; color:var(--text); background:#0f1d27; box-shadow:0 24px 90px rgba(0,0,0,.72); } dialog::backdrop { background:rgba(1,8,12,.78); backdrop-filter:blur(4px); } .dialog-body { padding:20px; } .dialog-body h2 { margin-bottom:5px; } .secure-note { border-left:3px solid var(--accent); padding:8px 10px; margin:14px 0; color:#cce7f5; background:#0b1820; font-size:12px; } .security-blocked { color:var(--warn); }
    @media (max-width:1080px) { header,main { padding-left:5vw; padding-right:5vw; } } @media (max-width:900px) { .grid { grid-template-columns:1fr; } .wide { grid-column:auto; } .two,.three,.run-grid,.history-tools { grid-template-columns:1fr; } .device-summary,.run-summary,.section-head { align-items:flex-start; flex-direction:column; } .history-latest { text-align:left; } }
    header { position:sticky; top:0; z-index:100; padding-top:16px; padding-bottom:14px; background:rgba(8,20,29,.96); backdrop-filter:blur(14px); box-shadow:0 9px 24px rgba(0,0,0,.38); } header h1 { font-size:25px; } .nav { margin-top:12px; }
  </style>
  <style>.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}.nct-brand{display:flex;width:max-content;flex-direction:column;align-items:center;margin:0 auto;color:var(--text);line-height:1;text-align:center}.nct-brand>strong{padding-left:.24em;font-size:40px;letter-spacing:.24em}.nct-brand span{margin-top:3px;padding-top:3px;border-top:1px solid var(--accent);color:var(--muted);font-size:9px;font-weight:650;letter-spacing:.09em;text-transform:uppercase;white-space:nowrap}.nct-brand span b{color:var(--accent);font-size:1.16em;font-weight:950}.nav{justify-content:center;margin-top:8px!important}.nav a{padding-top:6px!important;padding-bottom:6px!important}.activity-origin-banner{margin:8px auto -6px;padding:6px 10px 0;border-top:1px solid var(--line);color:var(--muted);font-size:11px;text-align:center}.activity-origin-banner strong{color:var(--accent);letter-spacing:.04em;text-transform:uppercase}.activity-origin-banner code{font-weight:850}</style>
</head>
<body>
  <header><h1 class="sr-only">Device</h1><div class="nct-brand" aria-label="NCT, Network Characterization Tool"><strong>NCT</strong><span><b>N</b>etwork <b>C</b>haracterization <b>T</b>ool</span></div><nav class="nav" aria-label="Primary"><a class="active" href="/device-config" aria-current="page">Device</a><a href="/scans">Nmap</a><a href="/analysis">Analyze</a><a href="/hunting">Hunt</a><a href="/network-map">Map</a></nav><div class="activity-origin-banner"><strong>Network activity origin</strong> · All device collection traffic started here originates from the NCT host at <code class="activity-origin-host">this host</code>.</div></header>
  <main>
    <div id="notice"></div>
    <div class="grid">
      <section class="panel">
        <h2>Build a collection plan</h2>
        <p class="hint">Start with the fixed vendor template, then add optional validated read-only commands. The password is used only to open a short-lived SSH session and is never saved.</p>
        <div class="two"><div><label for="operator">Operator name</label><input id="operator" placeholder="Analyst name"></div><div><label for="origin">Originating host</label><input id="origin" placeholder="kali-blue"></div></div>
        <label for="reason">Reason / authorization note</label><input id="reason" placeholder="Authorized configuration baseline…">
        <div class="two"><div><label for="vendor">Vendor</label><select id="vendor"><option value="vyos">VyOS</option><option value="cisco">Cisco</option><option value="juniper">Juniper</option><option value="pfsense">pfSense</option><option value="unifi">UniFi OS Gateway</option></select></div><div><label for="type">Device type</label><select id="type"><option value="router">Router</option><option value="firewall">Firewall</option></select></div></div><p id="vendorHint" class="hint"></p>
        <label for="discoveredDevice">Choose a device found by Nmap (optional)</label><select id="discoveredDevice"><option value="">Loading devices from previous scans…</option></select><p class="hint">Selecting a discovered host fills its IP address. You can still enter any authorized address manually.</p>
        <div class="two"><div><label for="deviceName">Device name (optional)</label><input id="deviceName" maxlength="100" placeholder="Core Router"></div><div><label for="address">Device address</label><input id="address" placeholder="192.0.2.1"></div></div>
        <div class="two"><div><label for="username">SSH username</label><input id="username" placeholder="operator"></div><div><label for="port">SSH port</label><input id="port" type="number" min="1" max="65535" value="22"></div></div>
        <label for="additionalCommands">Additional read-only commands (optional)</label><textarea id="additionalCommands" rows="5" placeholder="show arp&#10;show lldp neighbors detail"></textarea><p class="hint">Enter one command per line, up to 20. The tool blocks shell control characters and configuration-changing command families; every accepted command is shown in the preview and saved in the run record.</p>
        <label for="captureInterface">Accountability capture interface</label><select id="captureInterface"><option value="">Loading analyzer interfaces…</option></select><p class="hint">Required: tcpdump starts on this interface before every SSH access check and configuration pull. The resulting PCAP is retained as accountability evidence.</p>
        <div class="actions"><button id="preview">Generate command set</button><button id="startSsh" class="secondary" disabled>Start SSH and collect</button></div>
      </section>
      <section class="panel">
        <h2>Collection execution preview</h2>
        <div id="empty" class="meta">Enter the device details and generate a preview.</div>
        <div id="previewBox" class="hidden"><label>Complete read-only device command set</label><pre id="commands"></pre><div id="additionalPreviewBox" class="hidden"><label>Operator-added commands</label><pre id="additionalCommandPreview"></pre></div><label>Actual ordered execution plan</label><div id="executionPlan" class="execution-plan"></div><p class="hint">Every external command appears in run order. Local evidence writes are identified separately. Runtime-only paths are clearly marked, and no password is included.</p></div>
      </section>
      <section class="panel wide"><h2>Execution result</h2><div id="result" class="meta">No command set executed yet.</div><pre id="output" class="hidden"></pre><div id="liveArtifacts" class="artifacts hidden"></div></section>
    </div>
    <section class="panel wide" style="margin-top:18px"><h2>Upload existing configuration results</h2><p class="hint">Import a router or firewall configuration result collected elsewhere. The operator, reason, originating host, vendor, device type, and address are taken from the collection plan above. This local upload does not contact the device and therefore does not start tcpdump.</p><label for="uploadFile">Configuration result file (maximum 5 MB)</label><input id="uploadFile" type="file"><div class="actions"><button id="uploadResult" class="secondary">Upload result</button></div></section>
    <section class="panel wide" style="margin-top:18px"><div class="section-head"><div><h2>Networks identified in configuration files</h2><p class="hint">These are review-only suggestions from saved interface and routing data. Adding one makes it available in the Saved Networks selector; it does not start a scan.</p></div><button id="refreshNetworkCandidates" class="secondary" type="button">Refresh options</button></div><label for="configNetworkCandidate">Pending identified networks</label><select id="configNetworkCandidate"><option value="">Loading identified networks…</option></select><div id="configNetworkCandidateDetail" class="candidate-detail meta">Choose an identified network to review its source.</div><div class="actions"><button id="addNetworkCandidate" type="button">Add selected to Saved Networks</button></div><div id="networkCandidateStatus" class="meta" role="status" aria-live="polite"></div></section>
    <section class="panel wide" style="margin-top:18px"><div class="section-head"><div><h2>Previously collected network devices</h2><p class="hint">Open a collection to review structured interfaces, routes, neighbors, VLANs, policy, commands, configuration, and raw evidence. Select <strong>Use preset</strong> to restore the reusable connection details.</p></div><button id="refreshHistory" class="secondary">Refresh history</button></div><div class="history-tools"><div><label for="historySearch">Search collected results</label><input id="historySearch" type="search" placeholder="Device, address, route, interface, neighbor…"></div><div><label for="routeFilter">Route type</label><select id="routeFilter"><option value="all">All routes</option><option value="default">Default routes</option><option value="connected">Connected routes</option><option value="routed">Other routed networks</option></select></div></div><div id="historyStatus" class="meta">Loading device collection history…</div><div id="historyList" class="history-list"></div></section>
    <section class="panel wide" style="margin-top:18px"><h2>Interactive SSH workflow</h2><ol class="hint"><li>Review the complete read-only device command set and the ordered execution plan.</li><li>Start SSH. A private password dialog appears only after the device requests a password.</li><li>VyOS and pfSense create a named temporary output file, copy it to NCT, and remove it. Cisco, Juniper, and UniFi OS gateways return output directly through SSH and do not run SCP.</li><li>The password is never written to a manifest, history record, command line, environment variable, file, or application log.</li></ol><p class="hint"><strong>HTTPS is mandatory:</strong> password collection is disabled on plain HTTP so the password cannot cross the network unencrypted. The preview distinguishes commands run on the NCT host, commands sent to the device, and local evidence-file writes.</p></section>
    <div class="footer">Device collection uses fixed templates plus validated operator-added read-only commands. Review every command before execution.</div>
  </main>
  <dialog id="passwordDialog" aria-labelledby="passwordTitle"><div class="dialog-body"><h2 id="passwordTitle">SSH password required</h2><p id="passwordTarget" class="meta"></p><div class="secure-note">This one-time value goes directly to the waiting SSH process over HTTPS. It is cleared immediately and is not stored.</div><label for="sessionPassword">Device password</label><input id="sessionPassword" type="password" autocomplete="new-password" autocapitalize="none" spellcheck="false"><div class="actions"><button id="submitPassword">Connect and collect</button><button id="cancelPassword" class="secondary" type="button">Cancel and close SSH</button></div></div></dialog>
  <dialog id="deleteCollectionDialog" aria-labelledby="deleteCollectionTitle"><div class="dialog-body"><h2 id="deleteCollectionTitle">Delete this collection result?</h2><p id="deleteCollectionTarget" class="meta"></p><div class="secure-note">This removes this collection record and its saved evidence. Other device collections are not affected.</div><p>Enter <span id="deleteChallengeCode"></span> to confirm.</p><label for="deleteConfirmation">Confirmation code</label><input id="deleteConfirmation" autocomplete="off" autocapitalize="characters" spellcheck="false"><div class="actions"><button id="confirmDeleteCollection" class="danger" type="button">Delete result</button><button id="cancelDeleteCollection" class="secondary" type="button">Keep result</button></div><div id="deleteCollectionStatus" class="meta" role="status" aria-live="polite"></div></div></dialog>
<script>
const $ = id => document.getElementById(id);
let current = null;
let activeSshSession = null;
let networkCandidates = [];
let historyRecords = [];
const deviceAnalysisCache = new Map();
let pendingDeviceDelete = null;
function notice(message, kind='') { $('notice').textContent=message; $('notice').className=kind; }
function apiError(data, fallback) {
  const detail=data&&data.detail;
  if(typeof detail==='string') return detail;
  if(Array.isArray(detail)) return detail.map(item=>{
    const field=Array.isArray(item.loc)?item.loc.filter(part=>part!=='body').join('.') : '';
    return `${field?field+': ':''}${item.msg||'Invalid value'}`;
  }).join('; ');
  if(detail&&typeof detail==='object') return detail.message||JSON.stringify(detail);
  return fallback;
}
function payload() {
  const captureInterface=$('captureInterface').value;
  if(!captureInterface) throw new Error('Choose an accountability capture interface before contacting the device.');
  const additionalCommands=$('additionalCommands').value.split('\n').map(command=>command.trim()).filter(Boolean);
  return {operator:$('operator').value.trim(),originating_host:$('origin').value.trim()||location.hostname,reason:$('reason').value.trim(),vendor:$('vendor').value,device_type:$('type').value,device_address:$('address').value.trim(),device_name:$('deviceName').value.trim()||null,username:$('username').value.trim(),ssh_port:Number($('port').value),key_path:null,authentication_mode:'password_prompt',accountability_interface:captureInterface,additional_commands:additionalCommands};
}
function showPreview(data) { current=data; $('empty').classList.add('hidden'); $('previewBox').classList.remove('hidden'); $('commands').textContent=data.commands.join('\n'); const additions=data.additional_commands||[]; $('additionalCommandPreview').textContent=additions.join('\n'); $('additionalPreviewBox').classList.toggle('hidden',!additions.length); const plan=$('executionPlan'); plan.replaceChildren(); for(const [index,step] of (data.execution_steps||[]).entries()) { const card=element('div','execution-step'),head=element('div','execution-step-head'); head.append(element('span','execution-number',String(index+1)),element('span','execution-phase',step.phase||'Execution step'),element('span','execution-location',step.location||'NCT'),element('span','execution-kind',step.kind||'action')); card.append(head,element('pre','',step.command||'No command reported.')); if(step.detail) card.append(element('p','execution-detail',step.detail)); plan.append(card); } if(!plan.childElementCount) plan.append(element('div','meta','No ordered execution steps were returned.')); $('startSsh').disabled=!window.isSecureContext; if(!window.isSecureContext) notice('Preview ready. HTTPS must be enabled before a password can be entered.','warn'); }
function element(tag,className,text) { const node=document.createElement(tag); if(className) node.className=className; if(text!==undefined) node.textContent=text; return node; }
function formatTime(value) { if(!value) return 'Unknown time'; const date=new Date(value); return Number.isNaN(date.getTime())?value:date.toLocaleString(); }
function formatBytes(value) { if(value<1024) return `${value} B`; if(value<1024*1024) return `${(value/1024).toFixed(1)} KB`; return `${(value/1024/1024).toFixed(1)} MB`; }
function updateVendorHint() { const unifi=$('vendor').value==='unifi'; $('vendorHint').textContent=unifi?'UniFi OS gateways use a read-only Linux evidence set over SSH. Current UniFi consoles and gateways normally use the root SSH account; enable console SSH before collection. EdgeRouter devices should continue to use the VyOS template.':''; if(unifi&&!$('username').value.trim()) $('username').value='root'; }
function runField(label,value) { const box=element('div','run-field'); box.append(element('span','',label),element('strong','',value||'—')); return box; }
function showLiveArtifacts(artifacts) { const box=$('liveArtifacts'); box.replaceChildren(); for(const artifact of (artifacts||[])) { const link=element('a','',`${artifact.name} · ${formatBytes(artifact.size||0)}`); link.href=artifact.url; link.setAttribute('download',''); box.append(link); } box.classList.toggle('hidden',!box.childElementCount); }
function networkCandidateStatus(message,kind='') { const node=$('networkCandidateStatus'); node.textContent=message; node.className=kind||'meta'; }
function selectedNetworkCandidate() { return networkCandidates.find(item=>item.cidr===$('configNetworkCandidate').value)||null; }
function renderNetworkCandidateDetail() { const item=selectedNetworkCandidate(); if(!item) { $('configNetworkCandidateDetail').textContent=networkCandidates.length?'Choose an identified network to review its source.':'No pending config-derived networks.'; return; } const sources=(item.sources||[]).map(source=>{ const device=source.device_name||source.device_address||'saved configuration'; const place=source.zone?`zone ${source.zone}`:source.interface?`interface ${source.interface}`:source.kind||'routing data'; return `${device} · ${place}`; }); $('configNetworkCandidateDetail').textContent=`${item.cidr} · ${sources.join(' · ')}. This remains review-only until you add it.`; }
async function loadNetworkCandidates() { $('refreshNetworkCandidates').disabled=true; networkCandidateStatus('Checking saved configuration files…'); try { const r=await fetch('/api/device-configs/network-candidates'),data=await r.json(); if(!r.ok) throw new Error(apiError(data,'Identified networks could not be loaded')); networkCandidates=data.candidates||[]; const select=$('configNetworkCandidate'); select.replaceChildren(); if(networkCandidates.length) { for(const item of networkCandidates) { const option=element('option','',`${item.suggested_name}`); option.value=item.cidr; select.append(option); } networkCandidateStatus(`${networkCandidates.length} identified network option${networkCandidates.length===1?'':'s'} awaiting review.`); } else { const option=element('option','','No pending identified networks'); option.value=''; select.append(option); networkCandidateStatus('Every identified subnet is already saved, or no usable subnets were found.','good'); } renderNetworkCandidateDetail(); } catch(e) { networkCandidateStatus(e.message,'bad'); } finally { $('refreshNetworkCandidates').disabled=false; } }
function candidateSavedName(item) { const suffix=` · ${item.cidr}`; if(item.suggested_name.endsWith(suffix)) return item.suggested_name.slice(0,-suffix.length); return item.suggested_name===item.cidr?`Network ${item.cidr}`:item.suggested_name; }
async function addNetworkCandidate() { const item=selectedNetworkCandidate(),actor=$('operator').value.trim(); if(!item) { networkCandidateStatus('Choose an identified network first.','bad'); return; } if(!actor) { networkCandidateStatus('Enter the operator name above so the addition is recorded.','bad'); $('operator').focus(); return; } $('addNetworkCandidate').disabled=true; networkCandidateStatus('Adding the reviewed network…','warn'); try { const body={name:candidateSavedName(item),cidr:item.cidr,description:item.description||'',category:item.category||'Device configuration',tags:item.tags||['config-derived'],created_by:actor}; const r=await fetch('/api/saved-networks',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),data=await r.json(); if(!r.ok) throw new Error(apiError(data,'The network could not be saved')); await loadNetworkCandidates(); networkCandidateStatus(`${data.name} was added to Saved Networks and removed from this pending list.`,'good'); } catch(e) { networkCandidateStatus(e.message,'bad'); } finally { $('addNetworkCandidate').disabled=false; } }
function applyPreset(run) {
  $('vendor').value=run.vendor||'vyos'; $('type').value=run.device_type||'router'; $('deviceName').value=run.device_name||''; $('address').value=run.device_address||''; $('username').value=run.username||''; $('port').value=run.ssh_port||22; $('additionalCommands').value='';
  updateVendorHint();
  current=null; $('startSsh').disabled=true; $('previewBox').classList.add('hidden'); $('empty').classList.remove('hidden');
  notice(`Preset loaded for ${run.device_address}. Add the operator and authorization note, then generate the command set.`,'good');
  document.querySelector('main').scrollIntoView({behavior:'smooth',block:'start'}); $('operator').focus();
}
function searchTerm() { return $('historySearch').value.trim().toLowerCase(); }
function matchesSearch(value,term=searchTerm()) { return !term||JSON.stringify(value||'').toLowerCase().includes(term); }
function resultSection(title,items,columns,{open=false,empty='No matching evidence found.',total=null}={}) {
  const section=element('details','result-section'); section.open=open;
  const countText=total!==null&&total!==items.length?`${items.length} / ${total}`:String(items.length); const summary=element('summary',''); summary.append(document.createTextNode(title),element('span','count-badge',countText)); section.append(summary);
  const content=element('div','result-content');
  if(!items.length) content.append(element('div','result-empty',empty));
  else {
    const shown=items.slice(0,250); const availableTotal=total!==null?total:items.length; if(availableTotal>shown.length) content.append(element('p','result-note',`Showing ${shown.length} of ${availableTotal} rows${items.length<availableTotal?' from the bounded API preview':''}. Narrow the search to review more.`));
    const table=element('table','result-table'); const head=element('thead'); const headRow=element('tr'); for(const column of columns) headRow.append(element('th','',column.label)); head.append(headRow); table.append(head);
    const body=element('tbody'); for(const item of shown) { const row=element('tr'); for(const column of columns) { const value=typeof column.value==='function'?column.value(item):item[column.value]; row.append(element('td','',value===undefined||value===null||value===''?'—':String(value))); } body.append(row); } table.append(body); content.append(table);
  }
  section.append(content); return section;
}
function textResultSection(title,text,{open=false,truncated=false,filename=''}={}) {
  const section=element('details','result-section'); section.open=open; const lines=text?text.split('\n').length:0;
  const summary=element('summary',''); summary.append(document.createTextNode(title),element('span','count-badge',`${lines} lines`)); section.append(summary);
  const content=element('div','result-content'); if(filename||truncated) content.append(element('p','result-note',`${filename||'Saved evidence'}${truncated?' · preview limited to 2 MB':''}`));
  const term=searchTerm(); if(!text) content.append(element('div','result-empty','No saved text is available.')); else if(term&&!text.toLowerCase().includes(term)) content.append(element('div','result-empty','This text does not contain the current search.')); else content.append(element('pre','',text)); section.append(content); return section;
}
function renderStructuredResults(runId,target) {
  const data=deviceAnalysisCache.get(runId); target.replaceChildren(); if(!data) { target.append(element('div','meta','Open this result to load its structured evidence.')); return; }
  const term=searchTerm(),routeType=$('routeFilter').value;
  const filtered=value=>(value||[]).filter(item=>matchesSearch(item,term));
  const routes=(data.routes||[]).filter(route=>(routeType==='all'||route.route_type===routeType)&&matchesSearch(route,term));
  const neighbors=filtered([...(data.neighbors||[]),...(data.topology_neighbors||[])]);
  target.append(
    resultSection('Interfaces',filtered(data.interfaces),[{label:'Interface',value:'name'},{label:'Address',value:'address'},{label:'Zone / description',value:'zone'},{label:'MAC',value:'mac'}],{total:data.counts?.interfaces??null}),
    resultSection('Routes',routes,[{label:'Network',value:'network'},{label:'Type',value:'route_type'},{label:'Next hop',value:'via'},{label:'Interface',value:'interface'},{label:'Evidence',value:'line'}],{open:true,total:data.counts?.routes??null}),
    resultSection('Neighbors',neighbors,[{label:'Protocol',value:'protocol'},{label:'Neighbor / IP',value:item=>item.neighbor_name||item.ip||item.management_ip},{label:'MAC / chassis',value:item=>item.mac||item.chassis_mac||item.chassis_id},{label:'Local interface',value:item=>item.interface||item.local_interface},{label:'Remote port',value:'remote_port'}],{total:(data.counts?.neighbors??0)+(data.counts?.topology_neighbors??0)}),
    resultSection('VLANs',filtered(data.vlans),[{label:'Line',value:'line_number'},{label:'Evidence',value:'evidence'}],{total:data.counts?.vlans??null}),
    resultSection('Firewall / ACL',filtered(data.firewall_acl),[{label:'Line',value:'line_number'},{label:'Evidence',value:'evidence'}],{total:data.counts?.firewall_acl??null}),
    resultSection('NAT',filtered(data.nat),[{label:'Line',value:'line_number'},{label:'Evidence',value:'evidence'}],{total:data.counts?.nat??null}),
    resultSection('Network objects',filtered(data.network_objects),[{label:'Line',value:'line_number'},{label:'Evidence',value:'evidence'}],{total:data.counts?.network_objects??null}),
    resultSection('Commands',filtered((data.commands||[]).map((command,index)=>({number:index+1,command}))),[{label:'#',value:'number'},{label:'Command',value:'command'}],{total:data.counts?.commands??null}),
    textResultSection('Configuration',data.configuration_text||'',{filename:data.source_filename,truncated:data.configuration_truncated}),
    textResultSection('Raw output',data.raw_output||'',{filename:data.raw_filename,truncated:data.raw_truncated})
  );
}
function refreshStructuredResults() { for(const node of document.querySelectorAll('.structured-results[data-run-id]')) renderStructuredResults(node.dataset.runId,node); }
async function loadStructuredResults(runId,target) {
  if(deviceAnalysisCache.has(runId)) { renderStructuredResults(runId,target); return; }
  target.replaceChildren(element('div','meta','Loading structured interfaces, routes, neighbors, and policy evidence…'));
  try { const response=await fetch(`/api/device-configs/${runId}/summary`),data=await response.json(); if(!response.ok) throw new Error(apiError(data,'Structured results could not be loaded')); deviceAnalysisCache.set(runId,data); renderStructuredResults(runId,target); }
  catch(error) { target.replaceChildren(element('div','result-empty',error.message)); }
}
async function beginDeleteCollection(run,event) {
  event.preventDefault(); event.stopPropagation(); pendingDeviceDelete=run; $('deleteCollectionStatus').textContent='Requesting a confirmation code…'; $('deleteConfirmation').value='';
  try { const response=await fetch(`/api/device-configs/${run.run_id}/delete-challenge`,{method:'POST'}),data=await response.json(); if(!response.ok) throw new Error(apiError(data,'A delete confirmation could not be created')); $('deleteChallengeCode').textContent=data.challenge; $('deleteCollectionTarget').textContent=`${run.device_name?run.device_name+' · ':''}${run.device_address||'Unknown device'} · ${formatTime(run.completed_at||run.created_at)}`; $('deleteCollectionStatus').textContent=''; $('deleteCollectionDialog').showModal(); $('deleteConfirmation').focus(); }
  catch(error) { pendingDeviceDelete=null; notice(error.message,'bad'); }
}
function cancelDeleteCollection() { pendingDeviceDelete=null; $('deleteConfirmation').value=''; if($('deleteCollectionDialog').open) $('deleteCollectionDialog').close(); }
async function confirmDeleteCollection() {
  if(!pendingDeviceDelete) return; const confirmation=$('deleteConfirmation').value.trim(); if(!confirmation) { $('deleteCollectionStatus').textContent='Enter the confirmation code shown above.'; $('deleteConfirmation').focus(); return; }
  $('confirmDeleteCollection').disabled=true; $('cancelDeleteCollection').disabled=true; $('deleteCollectionStatus').textContent='Deleting this collection result…';
  try { const runId=pendingDeviceDelete.run_id,response=await fetch(`/api/device-configs/${runId}/delete`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({confirmation})}),data=await response.json(); if(!response.ok) throw new Error(apiError(data,'The collection result could not be deleted')); deviceAnalysisCache.delete(runId); cancelDeleteCollection(); notice('The selected device collection result and its saved evidence were deleted.','good'); await Promise.all([loadHistory(),loadNetworkCandidates()]); }
  catch(error) { $('deleteCollectionStatus').textContent=error.message; }
  finally { $('confirmDeleteCollection').disabled=false; $('cancelDeleteCollection').disabled=false; }
}
function renderHistory(records) {
  const list=$('historyList'); list.replaceChildren();
  const groups=new Map();
  for(const record of records) { const key=record.device_address||'Unknown device'; if(!groups.has(key)) groups.set(key,[]); groups.get(key).push(record); }
  if(!groups.size) { $('historyStatus').textContent='No network-device configuration records have been saved yet.'; return; }
  for(const runs of groups.values()) runs.sort((a,b)=>new Date(b.completed_at||b.created_at||0)-new Date(a.completed_at||a.created_at||0));
  $('historyStatus').textContent=`${groups.size} network device${groups.size===1?'':'s'} · ${records.length} configuration record${records.length===1?'':'s'}`;
  for(const [address,runs] of groups) {
    const device=element('details','device-history');
    const summary=element('summary','device-summary');
    const latest=runs[0]; const presetRecord=runs.find(run=>run.vendor&&run.device_type&&run.username)||latest;
    const label=latest.device_name?`${latest.device_name} · ${address}`:address;
    const left=element('div'); left.append(element('span','device-address',label),element('span','run-count',`${runs.length} run${runs.length===1?'':'s'}`)); const preset=element('button','preset','Use preset'); preset.type='button'; preset.onclick=event=>{event.preventDefault();event.stopPropagation();applyPreset(presetRecord);}; left.append(preset);
    summary.append(left,element('div','history-latest',`Latest: ${formatTime(latest.completed_at||latest.created_at)} · ${latest.status||'unknown'}`)); device.append(summary);
    const runList=element('div','run-list');
    for(const run of runs) {
      const card=element('details','run-card'); const runSummary=element('summary','run-summary');
      runSummary.append(element('span','',formatTime(run.completed_at||run.created_at)),element('span','',`${run.status||'unknown'} · ${run.vendor||''} ${run.device_type||''}`)); card.append(runSummary);
      const body=element('div','run-body'); const fields=element('div','run-grid');
      fields.append(runField('Device name',run.device_name),runField('IP / hostname',run.device_address),runField('Operator',run.operator),runField('Originating host',run.originating_host),runField('Reason / authorization',run.reason),runField('Exit code',run.exit_code===undefined?'—':String(run.exit_code)),runField('Username',run.username),runField('Run ID',run.run_id)); body.append(fields);
      if(run.remote_cleanup_status||run.remote_temp_created!==undefined) fields.append(runField('Remote temporary file',run.remote_temp_created?'Created for copy-back':'Not required'),runField('Cleanup status',run.remote_cleanup_status||'Not reported'));
      if(run.operation==='manual_upload') body.append(runField('Imported file',run.source_filename));
      if(Array.isArray(run.additional_commands)&&run.additional_commands.length) { body.append(element('label','','Operator-added commands'),element('pre','',run.additional_commands.join('\n'))); }
      if(Array.isArray(run.commands)&&run.commands.length) { body.append(element('label','','Commands executed'),element('pre','',run.commands.join('\n'))); }
      const artifacts=element('div','artifacts');
      for(const artifact of (run.artifacts||[])) { const link=element('a','',`${artifact.name} · ${formatBytes(artifact.size||0)}`); link.href=artifact.url; link.setAttribute('download',''); artifacts.append(link); }
      if(artifacts.childElementCount) body.append(element('label','','Saved files'),artifacts);
      const actions=element('div','run-actions'); const analyzeButton=element('button','secondary','Analyze'); analyzeButton.type='button'; analyzeButton.onclick=event=>{event.preventDefault();event.stopPropagation();location.href=`/device-analysis?run=${encodeURIComponent(run.run_id)}`;}; const deleteButton=element('button','danger','Delete result'); deleteButton.type='button'; deleteButton.onclick=event=>beginDeleteCollection(run,event); actions.append(analyzeButton,deleteButton); body.append(actions);
      const structured=element('div','structured-results'); structured.dataset.runId=run.run_id; body.append(structured); renderStructuredResults(run.run_id,structured);
      card.addEventListener('toggle',()=>{if(card.open) loadStructuredResults(run.run_id,structured);}); card.append(body); runList.append(card);
    }
    device.append(runList); list.append(device);
  }
}
async function loadHistory() { $('refreshHistory').disabled=true; $('historyStatus').textContent='Loading device collection history…'; try { const r=await fetch('/api/device-configs?limit=100'); const data=await r.json(); if(!r.ok) throw new Error(apiError(data,'Device collection history could not be loaded')); historyRecords=data; renderHistory(historyRecords); } catch(e) { $('historyStatus').textContent=e.message; } finally { $('refreshHistory').disabled=false; } }
function discoveredHostName(host) {
  if(host.hostname) return host.hostname;
  if(Array.isArray(host.hostnames)&&host.hostnames.length) {
    const first=host.hostnames[0]; return typeof first==='string'?first:(first&&first.name)||'';
  }
  return '';
}
async function loadDiscoveredDevices() {
  const select=$('discoveredDevice'); select.replaceChildren();
  const manual=element('option','','Enter an address manually'); manual.value=''; select.append(manual);
  try {
    const historyResponse=await fetch('/api/imports'); const imports=await historyResponse.json();
    if(!historyResponse.ok) throw new Error(apiError(imports,'Previous Nmap scans could not be loaded'));
    const details=await Promise.allSettled((imports||[]).slice(0,10).map(async item=>{
      const response=await fetch(`/api/imports/${item.sha256}`); const detail=await response.json();
      if(!response.ok) throw new Error('Scan details unavailable'); return detail;
    }));
    const hosts=new Map();
    for(const result of details) {
      if(result.status!=='fulfilled') continue;
      for(const host of (result.value.analysis&&result.value.analysis.hosts)||[]) {
        const address=host.ip||host.address||host.ipv4||host.ipv6;
        if(!address||hosts.has(address)) continue;
        hosts.set(address,{address,name:discoveredHostName(host),vendor:host.vendor||host.mac_vendor||''});
      }
    }
    for(const host of [...hosts.values()].sort((a,b)=>a.address.localeCompare(b.address,undefined,{numeric:true}))) {
      const description=host.name||host.vendor; const option=element('option','',description?`${description} · ${host.address}`:host.address);
      option.value=host.address; option.dataset.deviceName=host.name; select.append(option);
    }
    if(!hosts.size) manual.textContent='No saved Nmap devices yet — enter an address manually';
  } catch(_) { manual.textContent='Nmap device list unavailable — enter an address manually'; }
}
async function loadCaptureInterfaces() { try { const r=await fetch('/api/scan-runs/interfaces'); const data=await r.json(); if(!r.ok) throw new Error(apiError(data,'Analyzer interfaces could not be loaded')); const select=$('captureInterface'); select.replaceChildren(); for(const name of (data.interfaces||[])) { const option=element('option','',name); option.value=name; select.append(option); } if(!select.options.length) { const option=element('option','','No capture interfaces found'); option.value=''; select.append(option); } } catch(e) { notice(e.message,'bad'); } }
async function generate() { $('preview').disabled=true; notice('Generating command set…'); try { const r=await fetch('/api/device-configs/preview',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload())}); const data=await r.json(); if(!r.ok) throw new Error(apiError(data,'Preview could not be generated')); showPreview(data); notice(window.isSecureContext?'Preview ready. Review the commands before execution.':'Preview ready. HTTPS must be enabled before a password can be entered.',window.isSecureContext?'good':'warn'); } catch(e) { notice(e.message,'bad'); } finally { $('preview').disabled=false; } }
function displayCollection(data) { const target=data.device_name?`${data.device_name} · ${data.device_address}`:(data.device_address||''); $('result').textContent=`${data.status} · ${data.vendor||''} ${data.device_type||''} · ${target} · ${data.completed_at||''}`; $('output').classList.remove('hidden'); const stderr=(data.stderr||'').trim(); $('output').textContent=`COLLECTED OUTPUT\n${data.stdout||''}${stderr?`\n\nSTATUS DETAILS\n${stderr}`:'\n\nNo errors reported.'}`; showLiveArtifacts(data.artifacts); const completed=data.status==='completed'; const cleanupOk=!data.remote_cleanup_status||data.remote_cleanup_status==='completed'||data.remote_cleanup_status==='not_required'; notice(completed&&cleanupOk?'Collection, copy-back, cleanup, session closure, and accountability capture completed.':completed?'Collection completed, but remote cleanup could not be confirmed. Review the saved evidence.':'The SSH workflow did not complete. Review the status details.',completed&&cleanupOk?'good':completed?'warn':'bad'); loadHistory(); loadNetworkCandidates(); }
async function startInteractiveSsh() { if(!current||!window.isSecureContext) return; $('startSsh').disabled=true; notice('Opening SSH and waiting for the device password prompt…','warn'); $('result').textContent='Starting accountable SSH session…'; try { const r=await fetch('/api/device-configs/interactive/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload())}); const data=await r.json(); if(!r.ok) throw new Error(apiError(data,'SSH session could not be started')); if(data.status!=='waiting_for_password') { displayCollection(data); return; } activeSshSession=data.session_id; $('passwordTarget').textContent=`${data.username}@${data.device_address}:${data.ssh_port} · prompt expires in ${data.expires_in_seconds} seconds`; $('sessionPassword').value=''; $('passwordDialog').showModal(); $('sessionPassword').focus(); notice('SSH is waiting for the operator password.','warn'); } catch(e) { $('result').textContent=e.message; notice(e.message,'bad'); $('startSsh').disabled=false; } }
async function submitPassword() { if(!activeSshSession) return; const input=$('sessionPassword'); let password=input.value; if(!password) { input.focus(); return; } input.value=''; $('submitPassword').disabled=true; $('cancelPassword').disabled=true; $('passwordDialog').close(); notice('Authenticating, collecting evidence, copying it back, and cleaning up…','warn'); let requestBody=JSON.stringify({password}); password=''; try { const r=await fetch(`/api/device-configs/interactive/${activeSshSession}/password`,{method:'POST',headers:{'Content-Type':'application/json','Cache-Control':'no-store'},cache:'no-store',body:requestBody}); requestBody=''; const data=await r.json(); activeSshSession=null; if(!r.ok) throw new Error(apiError(data,'SSH authentication or collection failed')); displayCollection(data); } catch(e) { activeSshSession=null; $('result').textContent=e.message; notice(e.message,'bad'); } finally { requestBody=''; $('submitPassword').disabled=false; $('cancelPassword').disabled=false; $('startSsh').disabled=!window.isSecureContext; } }
async function cancelPassword() { const sessionId=activeSshSession; activeSshSession=null; $('sessionPassword').value=''; if($('passwordDialog').open) $('passwordDialog').close(); if(sessionId) { try { await fetch(`/api/device-configs/interactive/${sessionId}/cancel`,{method:'POST'}); } catch(_) {} } $('startSsh').disabled=!window.isSecureContext; notice('SSH session closed without storing a password.','warn'); }
async function uploadResult() { const file=$('uploadFile').files[0]; if(!file) { notice('Choose a configuration result file to upload.','warn'); return; } const required={operator:$('operator').value.trim(),reason:$('reason').value.trim(),originating_host:$('origin').value.trim()||location.hostname,vendor:$('vendor').value,device_type:$('type').value,device_address:$('address').value.trim(),device_name:$('deviceName').value.trim()}; if(!required.operator||!required.reason||!required.device_address) { notice('Operator, reason, and device address are required before uploading.','warn'); return; } const form=new FormData(); for(const [key,value] of Object.entries(required)) form.append(key,value); form.append('result_file',file,file.name); $('uploadResult').disabled=true; notice('Uploading the existing configuration result…','warn'); try { const r=await fetch('/api/device-configs/upload',{method:'POST',body:form}); const data=await r.json(); if(!r.ok) throw new Error(apiError(data,'Result upload failed')); showLiveArtifacts(data.artifacts); const target=data.device_name?`${data.device_name} · ${data.device_address}`:data.device_address; $('result').textContent=`uploaded · ${data.vendor} ${data.device_type} · ${target} · ${data.completed_at}`; $('uploadFile').value=''; notice('Configuration result uploaded and added to device history.','good'); await Promise.all([loadHistory(),loadNetworkCandidates()]); } catch(e) { notice(e.message,'bad'); } finally { $('uploadResult').disabled=false; } }
document.querySelectorAll('.activity-origin-host').forEach(node=>node.textContent=location.hostname); $('origin').value=location.hostname; $('preview').onclick=generate; $('startSsh').onclick=startInteractiveSsh; $('submitPassword').onclick=submitPassword; $('cancelPassword').onclick=cancelPassword; $('refreshHistory').onclick=loadHistory; $('uploadResult').onclick=uploadResult; $('refreshNetworkCandidates').onclick=loadNetworkCandidates; $('addNetworkCandidate').onclick=addNetworkCandidate; $('configNetworkCandidate').onchange=renderNetworkCandidateDetail; $('historySearch').oninput=refreshStructuredResults; $('routeFilter').onchange=refreshStructuredResults; $('confirmDeleteCollection').onclick=confirmDeleteCollection; $('cancelDeleteCollection').onclick=cancelDeleteCollection; $('vendor').onchange=updateVendorHint;
$('discoveredDevice').onchange=()=>{ const option=$('discoveredDevice').selectedOptions[0]; if(!option||!option.value) return; $('address').value=option.value; if(option.dataset.deviceName&&!$('deviceName').value.trim()) $('deviceName').value=option.dataset.deviceName; };
$('passwordDialog').addEventListener('cancel',event=>{event.preventDefault();cancelPassword();}); $('sessionPassword').addEventListener('keydown',event=>{if(event.key==='Enter'){event.preventDefault();submitPassword();}});
$('deleteCollectionDialog').addEventListener('cancel',event=>{event.preventDefault();cancelDeleteCollection();}); $('deleteConfirmation').addEventListener('keydown',event=>{if(event.key==='Enter'){event.preventDefault();confirmDeleteCollection();}});
if(!window.isSecureContext) notice('Interactive SSH password entry is disabled until this analyzer is served over HTTPS.','warn');
loadHistory();
loadNetworkCandidates();
loadCaptureInterfaces();
loadDiscoveredDevices();
updateVendorHint();
</script>
</body></html>'''
    )
